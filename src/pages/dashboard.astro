---
import MyChart from '../components/MyChart.jsx';
---

<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>My Dashboard</title>
    <style>
        body { font-family: sans-serif; line-height: 1.6; max-width: 800px; margin: 2rem auto; }
        .spacer { height: 150vh; background: #f0f4f8; border-radius: 8px; display: grid; place-items: center; color: #555; }
    </style>
</head>
<body>
    <h1>My Dashboard</h1>
    <p>
        This page demonstrates Astro Islands. The page itself is static HTML, so it loads instantly.
    </p>
    <p>
        Scroll down to see the interactive chart. Its JavaScript will only be loaded when it enters the viewport.
    </p>

    <div class="spacer">
        <p>Scroll down...</p>
    </div>

    {/* 
      This is the Astro Island!
      The 'client:visible' directive tells Astro to only hydrate this component 
      (load its JavaScript and make it interactive) when it becomes visible in the viewport.
    */}
    <MyChart client:visible />

</body>
</html>
